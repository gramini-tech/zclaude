{
  "name": "zclaude",
  "version": "0.1.1",
  "description": "Interactive preloader for Claude Code: pick your Anthropic account or your Z.ai GLM Coding Plan per launch, with browser sign-in, Keychain-stored keys and per-project model config",
  "type": "module",
  "bin": {
    "zclaude": "bin/zclaude.js"
  },
  "files": [
    "bin",
    "src",
    "zclaude",
    "README.md",
    "LICENSE",
    "install.sh"
  ],
  "engines": {
    "node": ">=20.17.0"
  },
  "scripts": {
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-lines=80 --test-coverage-branches=75 --test-coverage-functions=75 --test-coverage-include='src/**' --test-coverage-include='bin/**' --test-coverage-exclude='src/ui/menu.js' --test-coverage-exclude='src/ui/wizard.js' --test-coverage-exclude='src/callback/paste.js'",
    "lint": "npm run -s lint:js && npm run -s lint:format && npm run -s lint:dead && npm run -s typecheck && npm run -s lint:sh",
    "lint:js": "eslint .",
    "lint:format": "prettier --check .",
    "lint:dead": "knip --no-progress",
    "typecheck": "tsc -p jsconfig.json",
    "lint:sh": "sh scripts/lint-shell.sh",
    "lint:fix": "eslint . --fix && prettier --write .",
    "check": "npm run -s lint && npm run -s test:coverage && npm pack --dry-run",
    "prepare": "node scripts/install-hooks.js"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.7.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vipincr/zclaude.git"
  },
  "homepage": "https://github.com/vipincr/zclaude#readme",
  "bugs": {
    "url": "https://github.com/vipincr/zclaude/issues"
  },
  "keywords": [
    "claude-code",
    "z.ai",
    "glm",
    "coding-plan",
    "cli",
    "oauth",
    "preloader"
  ],
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.20.3",
    "eslint": "^10.10.0",
    "eslint-plugin-n": "^18.3.0",
    "eslint-plugin-promise": "^7.3.0",
    "eslint-plugin-security": "^4.0.1",
    "eslint-plugin-sonarjs": "^4.2.1",
    "eslint-plugin-unicorn": "^75.0.0",
    "globals": "^17.12.0",
    "knip": "^5.88.1",
    "prettier": "^3.9.7",
    "typescript": "^5.9.3"
  }
}
